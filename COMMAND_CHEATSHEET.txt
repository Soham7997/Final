╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                    🚀 COMMAND REFERENCE CHEATSHEET 🚀                    ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         START BOTH SERVERS (Choose ONE)                  ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

🖱️  METHOD 1: Windows - Easiest (Double-Click)
   ┌────────────────────────────────────────────────────────┐
   │  File: RUN_SERVERS.bat                                 │
   │  Action: Double-click the file in File Explorer        │
   │  Result: Both servers start automatically              │
   └────────────────────────────────────────────────────────┘

📱 METHOD 2: Windows - Batch (Command Line)
   ┌────────────────────────────────────────────────────────┐
   │  Command: start_all.bat                                │
   │  Where: Run in Command Prompt (cmd.exe)               │
   │  Result: Both servers start in separate windows       │
   └────────────────────────────────────────────────────────┘

🐚 METHOD 3: PowerShell
   ┌────────────────────────────────────────────────────────┐
   │  Command: .\start_all.ps1                              │
   │  Where: Run in PowerShell                              │
   │  Note: May need: Set-ExecutionPolicy -Bypass           │
   │  Result: Both servers start with logging               │
   └────────────────────────────────────────────────────────┘

🐍 METHOD 4: Python (Cross-Platform)
   ┌────────────────────────────────────────────────────────┐
   │  Command: python start_all.py                          │
   │  Where: Run in any terminal                            │
   │  Works On: Windows, Linux, macOS                       │
   │  Result: Both servers start with monitoring            │
   └────────────────────────────────────────────────────────┘

⚙️  METHOD 5: Manual (If Scripts Fail)
   ┌────────────────────────────────────────────────────────┐
   │  Terminal 1:                                            │
   │    $ conda activate body_tracking                      │
   │    $ python server.py                                  │
   │                                                        │
   │  Terminal 2:                                            │
   │    $ conda activate body_tracking                      │
   │    $ python run_dev.py                                 │
   │                                                        │
   │  Result: Both servers run independently                │
   └────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                              OPEN IN BROWSER                              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

Main Portal (Port 5000):
   ┌─────────────────────────────────────────────────────────┐
   │  http://localhost:5000                                  │
   │                                                         │
   │  What You'll See:                                        │
   │  • Drone Tech AI Portal login page                       │
   │  • Login form with Name, Email, Password                │
   │  • Sign In / Log In buttons                             │
   └─────────────────────────────────────────────────────────┘

Body Tracking (Port 8000):
   ┌─────────────────────────────────────────────────────────┐
   │  http://localhost:8000                                  │
   │                                                         │
   │  What You'll See:                                        │
   │  • Body Tracking AI Interface                           │
   │  • Video feed from webcam                               │
   │  • Start/Stop tracking buttons                          │
   │  • Metrics panel                                        │
   └─────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         STOP BOTH SERVERS                                 ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

METHOD 1: Keyboard Shortcut (Recommended)
   ┌─────────────────────────────────────────────────────────┐
   │  Press: Ctrl + C                                        │
   │  In: The terminal running the startup script            │
   │  Result: Both servers shut down gracefully              │
   │  Output: "All servers stopped"                          │
   └─────────────────────────────────────────────────────────┘

METHOD 2: Close Terminal Window
   ┌─────────────────────────────────────────────────────────┐
   │  Close: The terminal/batch window                       │
   │  Result: Servers stop automatically                     │
   │  Note: Works for any startup method                     │
   └─────────────────────────────────────────────────────────┘

METHOD 3: Task Manager (Windows)
   ┌─────────────────────────────────────────────────────────┐
   │  Open: Task Manager (Ctrl+Shift+Esc)                    │
   │  Find: python.exe processes                             │
   │  Right-Click: End Task                                  │
   │  Result: Processes terminate                            │
   │  Note: Not graceful, use Ctrl+C if possible             │
   └─────────────────────────────────────────────────────────┘

METHOD 4: Command Line
   ┌─────────────────────────────────────────────────────────┐
   │  Windows:                                                │
   │    taskkill /F /IM python.exe                           │
   │                                                         │
   │  Linux/macOS:                                            │
   │    pkill -f "python server.py"                          │
   │    pkill -f "python run_dev.py"                         │
   │                                                         │
   │  Note: Force kill, not graceful                         │
   └─────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                       HELPFUL COMMANDS                                    ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

Check if Conda is Installed:
   ┌─────────────────────────────────────────────────────────┐
   │  $ conda --version                                      │
   │  Output: conda 4.x.x                                    │
   └─────────────────────────────────────────────────────────┘

List Conda Environments:
   ┌─────────────────────────────────────────────────────────┐
   │  $ conda env list                                       │
   │  Look for: body_tracking (should be listed)             │
   └─────────────────────────────────────────────────────────┘

Activate Conda Environment:
   ┌─────────────────────────────────────────────────────────┐
   │  $ conda activate body_tracking                         │
   │  Output: (body_tracking) $ prompt                       │
   └─────────────────────────────────────────────────────────┘

Deactivate Conda Environment:
   ┌─────────────────────────────────────────────────────────┐
   │  $ conda deactivate                                     │
   │  Output: $ prompt (back to base)                        │
   └─────────────────────────────────────────────────────────┘

Install Requirements:
   ┌─────────────────────────────────────────────────────────┐
   │  $ conda activate body_tracking                         │
   │  $ pip install -r requirements.txt                      │
   └─────────────────────────────────────────────────────────┘

Check Python Version:
   ┌─────────────────────────────────────────────────────────┐
   │  $ python --version                                     │
   │  Output: Python 3.8.x (or higher)                       │
   └─────────────────────────────────────────────────────────┘

Check Port Usage (Windows):
   ┌─────────────────────────────────────────────────────────┐
   │  $ netstat -ano | findstr :5000                         │
   │  $ netstat -ano | findstr :8000                         │
   │  Shows: PID of process using the port                   │
   └─────────────────────────────────────────────────────────┘

Check Port Usage (Linux/macOS):
   ┌─────────────────────────────────────────────────────────┐
   │  $ lsof -i :5000                                        │
   │  $ lsof -i :8000                                        │
   │  Shows: Process using the port                          │
   └─────────────────────────────────────────────────────────┘

Kill Process by Port (Windows):
   ┌─────────────────────────────────────────────────────────┐
   │  $ netstat -ano | findstr :5000                         │
   │  $ taskkill /PID <PID> /F                               │
   │  Replace <PID> with the process ID                      │
   └─────────────────────────────────────────────────────────┘

Kill Process by Port (Linux/macOS):
   ┌─────────────────────────────────────────────────────────┐
   │  $ lsof -ti :5000 | xargs kill -9                       │
   │  $ lsof -ti :8000 | xargs kill -9                       │
   └─────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                        TROUBLESHOOTING COMMANDS                           ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

If "Port already in use" error:
   ┌─────────────────────────────────────────────────────────┐
   │  1. Check what's using the port:                        │
   │     Windows: netstat -ano | findstr :5000              │
   │     Linux:   lsof -i :5000                             │
   │                                                         │
   │  2. Kill the process:                                   │
   │     Windows: taskkill /PID <PID> /F                     │
   │     Linux:   kill -9 <PID>                              │
   │                                                         │
   │  3. Try starting servers again                          │
   └─────────────────────────────────────────────────────────┘

If Module Import Error:
   ┌─────────────────────────────────────────────────────────┐
   │  1. Activate environment:                               │
   │     $ conda activate body_tracking                      │
   │                                                         │
   │  2. Upgrade pip:                                        │
   │     $ pip install --upgrade pip                         │
   │                                                         │
   │  3. Reinstall requirements:                             │
   │     $ pip install -r requirements.txt --upgrade         │
   │                                                         │
   │  4. Try starting servers again                          │
   └─────────────────────────────────────────────────────────┘

If "conda: command not found":
   ┌─────────────────────────────────────────────────────────┐
   │  1. Install Anaconda/Miniconda                          │
   │     From: https://www.anaconda.com/download/           │
   │                                                         │
   │  2. Restart Terminal/CMD                               │
   │                                                         │
   │  3. Try again: conda --version                          │
   └─────────────────────────────────────────────────────────┘

If "environment not found":
   ┌─────────────────────────────────────────────────────────┐
   │  1. Create environment:                                 │
   │     $ conda create -n body_tracking python=3.8          │
   │                                                         │
   │  2. Activate:                                           │
   │     $ conda activate body_tracking                      │
   │                                                         │
   │  3. Install requirements:                               │
   │     $ pip install -r requirements.txt                   │
   │                                                         │
   │  4. Try starting servers again                          │
   └─────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         DIRECTORY NAVIGATION                              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

Go to Project Directory:
   ┌─────────────────────────────────────────────────────────┐
   │  $ cd c:\Users\soham\Desktop\Final                      │
   └─────────────────────────────────────────────────────────┘

List Files in Current Directory:
   ┌─────────────────────────────────────────────────────────┐
   │  $ dir (Windows)                                        │
   │  $ ls (Linux/macOS)                                     │
   └─────────────────────────────────────────────────────────┘

Show Current Directory:
   ┌─────────────────────────────────────────────────────────┐
   │  $ cd (Windows)                                         │
   │  $ pwd (Linux/macOS)                                    │
   └─────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         EDITOR/IDE COMMANDS                               ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

Open in VS Code:
   ┌─────────────────────────────────────────────────────────┐
   │  $ code .                                               │
   │  (from the project directory)                           │
   └─────────────────────────────────────────────────────────┘

Open Specific File in VS Code:
   ┌─────────────────────────────────────────────────────────┐
   │  $ code script.js                                       │
   │  $ code server.py                                       │
   └─────────────────────────────────────────────────────────┘

Open Terminal in VS Code:
   ┌─────────────────────────────────────────────────────────┐
   │  Keyboard: Ctrl + `                                     │
   │  Menu: View → Terminal                                  │
   └─────────────────────────────────────────────────────────┘

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃                         QUICK START COMMANDS                              ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

Complete Quick Start (Copy & Paste):
   ┌─────────────────────────────────────────────────────────┐
   │  1. Navigate to project:                                │
   │     cd c:\Users\soham\Desktop\Final                     │
   │                                                         │
   │  2. Activate environment:                               │
   │     conda activate body_tracking                        │
   │                                                         │
   │  3. Start both servers:                                 │
   │     python start_all.py                                 │
   │                                                         │
   │  4. Open browser:                                       │
   │     http://localhost:5000                               │
   │                                                         │
   │  5. To stop:                                            │
   │     Press Ctrl+C in the terminal                        │
   └─────────────────────────────────────────────────────────┘

╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║                      🎉 YOU'RE ALL SET! 🎉                               ║
║                                                                            ║
║              For Detailed Help → See DOCUMENTATION_INDEX.md              ║
║              For Visual Guide → See QUICK_REFERENCE.txt                  ║
║              For Setup Help → See STARTUP_GUIDE.md                       ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝
